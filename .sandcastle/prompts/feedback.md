# Feedback Worker Instructions

Reuse the same pull request branch.

Address only blocking gate feedback. Keep changes minimal and do not create a new pull request.

Do not create or modify secrets, generated files, build outputs, or logs.

Run:

```sh
npm run typecheck
npm run build
```

Commit your changes with a clear message.
