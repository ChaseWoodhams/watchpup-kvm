# WatchPup KVM

WatchPup KVM is a fresh clean baseline project.

## Local Agent Workflow

This repository uses `agent-loop-kit` and Sandcastle for issue-based agent development with project-only configuration stored in the repo.

Agents may open pull requests, but humans review and merge them.
